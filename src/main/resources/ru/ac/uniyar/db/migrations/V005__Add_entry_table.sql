CREATE TABLE ENTRY (
    ID INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    DATE_CREATION TIMESTAMP NOT NULL,
    ANIMAL_ID INT NOT NULL,
    TEXT VARCHAR(200) NOT NULL,
    FOR_TRUSTEE BIT NOT NULL
);

ALTER TABLE ENTRY ADD CONSTRAINT ENTRY_ANIMALS_FK FOREIGN KEY (ANIMAL_ID) REFERENCES ANIMALS (ID);