CREATE TABLE TRUSTEE_TO_ANIMAL (
    ID INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID INT NOT NULL,
    ANIMAL_ID INT NOT NULL,
    DATE_CREATION TIMESTAMP NOT NULL
);

ALTER TABLE TRUSTEE_TO_ANIMAL ADD CONSTRAINT TRUSTEE_TO_ANIMAL_ANIMALS_FK FOREIGN KEY (ANIMAL_ID) REFERENCES ANIMALS (ID);
ALTER TABLE TRUSTEE_TO_ANIMAL ADD CONSTRAINT TRUSTEE_TO_ANIMAL_USERS_FK FOREIGN KEY (USER_ID) REFERENCES USERS (ID);